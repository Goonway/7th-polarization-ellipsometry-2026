# 网站内容更新操作指南

> 每次添加新图片或修改 HTML 内容后，按以下 4 步操作即可同步到线上。

---

## 第 1 步：压缩新图片（可选但推荐）

在终端中运行 `sips` 命令，将图片缩放到合适尺寸：

```bash
# 头像照片缩放到 320px（PNG 格式）
sips -Z 320 -s format png 新图片.png --out 输出.png

# 风景/场景照片缩放到 800px（JPG 格式，质量70%）
sips -Z 800 -s formatOptions 70 新图片.jpg --out 输出.jpg

# LOGO 图片缩放到 600px
sips -Z 600 -s format png -s formatOptions 70 新LOGO.png --out 输出.png
```

**尺寸参考：**
| 图片类型 | 推荐尺寸 | 格式 |
|---------|---------|------|
| 人物头像 | 320px | PNG |
| 酒店照片 | 800px | JPG |
| LOGO 标志 | 400-600px | PNG |
| 校徽/大图 | 600px | JPG |

---

## 第 2 步：运行辅助脚本

将新图片放入 `images/` 对应目录，修改好 HTML 后，运行：

```bash
cd ~/WorkBuddy/2026-07-05-00-29-22/conference-website
python3 update_cdn.py
```

脚本会自动完成：
- 为新增的 `<img>` 标签添加 `loading="lazy"` 懒加载属性
- 将新增的相对路径（`images/`、`css/`、`js/`、`downloads/`）替换为 jsDelivr CDN 绝对路径

---

## 第 3 步：提交并推送到 GitHub

```bash
cd ~/WorkBuddy/2026-07-05-00-29-22/conference-website
git add -A
git commit -m "更新专题X邀请报告"
git push
```

> 提交信息按实际修改内容填写，如"新增专题三报告人照片"等。

---

## 第 4 步：清除 jsDelivr CDN 缓存

推送后 **等待 1-2 分钟**（让 GitHub 同步到 jsDelivr），然后在浏览器中访问以下地址触发缓存刷新：

```
https://purge.jsdelivr.net/gh/Goonway/7th-polarization-ellipsometry-2026@main/images/专题1/新图片.png
```

**把路径换成你新增/修改的文件路径即可。** 如果改了多个文件，逐个访问刷新。

### 常见路径示例

| 修改内容 | 缓存刷新 URL |
|---------|-------------|
| CSS 样式 | `https://purge.jsdelivr.net/gh/Goonway/7th-polarization-ellipsometry-2026@main/css/style.css` |
| JS 脚本 | `https://purge.jsdelivr.net/gh/Goonway/7th-polarization-ellipsometry-2026@main/js/main.js` |
| 专题图片 | `https://purge.jsdelivr.net/gh/Goonway/7th-polarization-ellipsometry-2026@main/images/专题5/张三.png` |
| 下载文件 | `https://purge.jsdelivr.net/gh/Goonway/7th-polarization-ellipsometry-2026@main/downloads/摘要模板.docx` |

> 刷新后会返回一段 JSON，包含 `status: "ok"` 表示缓存已清除。

---

## 快速参考（一行命令版）

如果你只改了 HTML 没加新图片，可以跳过第 1 步和第 4 步：

```bash
cd ~/WorkBuddy/2026-07-05-00-29-22/conference-website && python3 update_cdn.py && git add -A && git commit -m "更新内容" && git push
```

如果加了新图片，完整四步：

```bash
# 第1步：压缩（按需修改文件名和尺寸）
sips -Z 320 -s format png 新图片.png --out images/专题5/新图片.png

# 第2-3步：更新CDN链接 + 提交推送
cd ~/WorkBuddy/2026-07-05-00-29-22/conference-website && python3 update_cdn.py && git add -A && git commit -m "新增专题5报告人" && git push

# 第4步：浏览器打开刷新缓存（等待1-2分钟后）
# https://purge.jsdelivr.net/gh/Goonway/7th-polarization-ellipsometry-2026@main/images/专题5/新图片.png
```
