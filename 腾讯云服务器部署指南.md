# 腾讯云服务器部署指南

> 适用于：不确定操作系统、暂时无域名、通过腾讯云控制台登录的用户

---

## 第一步：确认服务器操作系统

1. 登录 [腾讯云控制台](https://console.cloud.tencent.com/)
2. 进入 **云服务器 CVM** → 找到您的实例
3. 查看「操作系统」一列，记下是 Ubuntu、CentOS 还是其他

或者通过控制台 **登录实例**（点击「登录」按钮），在终端输入：

```bash
cat /etc/os-release
```

---

## 第二步：开放 80 端口（HTTP）

1. 在腾讯云控制台 → 云服务器 → 点击您的实例名
2. 找到 **安全组** 标签页
3. 点击绑定的安全组 → **编辑规则**
4. **入站规则** 中添加：
   - 来源：`0.0.0.0/0`
   - 协议端口：`TCP:80`
   - 策略：允许
5. 如果有 443 端口也一并开放（以后配 HTTPS 用）

---

## 第三步：安装 Nginx

通过腾讯云控制台 **登录实例**，在终端执行：

### 如果是 Ubuntu / Debian：
```bash
apt update
apt install nginx -y
```

### 如果是 CentOS：
```bash
yum install nginx -y
```

安装完成后，启动 Nginx：
```bash
systemctl start nginx
systemctl enable nginx    # 设置开机自启
```

此时在浏览器访问 `http://您的服务器IP`，应该能看到 Nginx 欢迎页面。

---

## 第四步：上传网站文件

### 方法 A：通过 Git 拉取（推荐，最简单）

如果您的代码已推送到 GitHub，在服务器上执行：

```bash
# 安装 git（如果没有）
# Ubuntu: apt install git -y
# CentOS: yum install git -y

git clone https://github.com/Goonway/7th-polarization-ellipsometry-2026.git /var/www/conference
```

> 注意：GitHub 在国内服务器上可能较慢。如果拉取失败，用方法 B。

### 方法 B：从本地电脑上传（SCP）

在您**本地电脑**的终端（不是服务器上）执行：

```bash
# 将整个网站目录上传到服务器
scp -r /Users/wanfushen/WorkBuddy/2026-07-05-00-29-22/conference-website/* root@您的服务器IP:/var/www/conference/
```

> 如果提示输入密码，输入服务器 root 密码即可。
> 如果不知道 root 密码，可以在腾讯云控制台重置密码。

### 方法 C：先打包再上传（文件较多时更稳）

在**本地电脑**终端执行：
```bash
# 打包
cd /Users/wanfushen/WorkBuddy/2026-07-05-00-29-22
tar czf conference.tar.gz conference-website/

# 上传
scp conference.tar.gz root@您的服务器IP:/root/
```

然后在**服务器**终端执行：
```bash
mkdir -p /var/www/conference
tar xzf /root/conference.tar.gz -C /var/www/conference --strip-components=1
```

---

## 第五步：配置 Nginx 指向网站

在服务器终端执行：

```bash
# 备份默认配置
cp /etc/nginx/nginx.conf /etc/nginx/nginx.conf.bak
```

然后编辑配置文件：

```bash
cat > /etc/nginx/nginx.conf << 'EOF'
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log;
pid /run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    keepalive_timeout 65;

    # Gzip 压缩（加快传输）
    gzip on;
    gzip_types text/css application/javascript text/html image/svg+xml;

    server {
        listen 80;
        server_name _;

        root /var/www/conference;
        index index.html;

        # 静态资源缓存（图片缓存30天）
        location ~* \.(jpg|jpeg|png|gif|ico|css|js|pdf|docx)$ {
            expires 30d;
            add_header Cache-Control "public, no-transform";
        }

        location / {
            try_files $uri $uri/ =404;
        }
    }
}
EOF
```

> 如果是 Ubuntu，`user` 改为 `www-data;`，`pid` 改为 `/run/nginx.pid;`

测试配置并重启：
```bash
nginx -t          # 检查配置语法
systemctl restart nginx
```

---

## 第六步：设置文件权限

```bash
chown -R nginx:nginx /var/www/conference
chmod -R 755 /var/www/conference
```

> 如果是 Ubuntu，`nginx:nginx` 改为 `www-data:www-data`

---

## 第七步：访问验证

在浏览器输入：
```
http://您的服务器公网IP
```

应该能看到网站首页。逐个点击页面检查图片和功能是否正常。

---

## 后续更新网站内容

每次修改网站后，重新上传文件即可：

### 如果用 Git：
```bash
# 本地推送后，在服务器执行
cd /var/www/conference
git pull
```

### 如果用 SCP：
```bash
# 在本地终端执行
scp -r /Users/wanfushen/WorkBuddy/2026-07-05-00-29-22/conference-website/* root@您的服务器IP:/var/www/conference/
```

---

## 常见问题

### Q: 访问不了网站？
1. 检查安全组是否开放 80 端口
2. 检查 Nginx 是否运行：`systemctl status nginx`
3. 检查文件路径：`ls /var/www/conference/index.html`

### Q: 图片显示不了？
- 检查文件权限：`chmod -R 755 /var/www/conference`
- 检查文件是否完整上传

### Q: 以后想加域名？
1. 购买域名（腾讯云可买）
2. 添加 A 记录指向服务器 IP
3. 国内服务器需完成**网站备案**（约 7-20 个工作日）
4. 备案通过后，修改 Nginx 的 `server_name` 为您的域名

### Q: 以后想加 HTTPS？
```bash
# 安装 certbot
# Ubuntu: apt install certbot python3-certbot-nginx -y
# CentOS: yum install certbot python3-certbot-nginx -y

# 自动配置 HTTPS 证书（需要先配好域名）
certbot --nginx -d 您的域名
```
